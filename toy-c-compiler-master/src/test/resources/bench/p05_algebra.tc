int main() {
    int i = 0;
    int s = 0;
    while (i < 7000000) {
        int x = i % 239;
        int y = (((x * 1 + 0) - 0) * 2) / 2;
        int z = (y + y) - (x * 1) - y + (0 * i);
        s = (s + z + x - y + 5) % 256;
        i = i + 1;
    }
    return s;
}
